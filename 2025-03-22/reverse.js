






function reverse(number) {
    let rstring = number.toString()
    
    for (let index = rstring.length; index > 0; index = index-1) {
        console.log (rstring[index-1])
    }

    // for loop and print
}

reverse(635502123)